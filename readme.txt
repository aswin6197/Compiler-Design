gcd.c contains a C program with for loop int, return and printf

palindrome is a C program which has a while loop

Both the programs contain keywords like int,identifiers,if statements,return,main function along with the necessary delimiters and brackets

error.c is a factorial program which doesnot have the necessary closing syntax for multi line comments
